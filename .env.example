# Anthropic API key — used by the exception classification / decision agent
# and the "Ask the Agent" chat endpoint. The app runs fine without it using
# a deterministic rule-based fallback; add this for full LLM reasoning.
ANTHROPIC_API_KEY=your_key_here

PORT=4000
